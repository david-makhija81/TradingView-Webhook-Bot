---
version: '3.9'
services:
    tradingview-webhook-bot:
        build: .
        container_name: tradingview-webhook-bot
        ports:
            - '80:8080'
        env_file:
            - .env
        restart: unless-stopped
